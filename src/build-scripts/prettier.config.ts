import {type Config} from 'prettier';

const config: Config = {
    arrowParens: 'always',
    semi: true,
    endOfLine: 'lf',
    printWidth: 140,
    tabWidth: 4,
    quoteProps: 'as-needed',
    trailingComma: 'es5',
    useTabs: false,
};

export default config;
