package inventory

//revive:disable:line-length-limit
const (
    strYaml                   = "yaml"
    strJson                   = "json"
    strXml                    = "xml"
    strCsv                    = "csv"
    strInvalidFormat          = "invalid report format"
    strFailedCreatingFilename = "Failed to generate filename: %s"
    strOutputDirIsFile        = "%s is a file, not a directory"
)
