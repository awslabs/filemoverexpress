package systeminfo

//revive:disable:line-length-limit
const (
    strMaxActiveChecksumsTooHigh = "can't set max active checksums (%d) higher than %d. Capping at this value"
    strFailedRetrievingMachineId = "failed to start application: %s"
)
