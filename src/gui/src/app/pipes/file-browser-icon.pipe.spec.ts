import { FileBrowserIconPipe } from './file-browser-icon.pipe';

describe('FileBrowserIconPipe', () => {
    it('create an instance', () => {
        const pipe = new FileBrowserIconPipe();
        expect(pipe).toBeTruthy();
    });
});
